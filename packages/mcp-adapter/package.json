{
  "name": "@failure-report/mcp-adapter",
  "version": "0.1.0",
  "description": "MCP stdio wrapper for FailureReport through its default Eve Channel.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "mcp": "node dist/stdio.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@failure-report/protocol": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "eve": "^0.24.4",
    "zod": "^4.4.3"
  }
}
