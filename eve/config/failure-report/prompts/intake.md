# Intake Prompt

Normalize the incoming failure without prematurely diagnosing it. Preserve the reported symptom, expected behavior, environment, target revision, reproduction signals, artifacts, and source provenance. A diagnostic target must use a canonical repository identity and full immutable Git SHA; do not record `HEAD`, a branch/tag selector, or a local checkout path. Adopt or create the target-repository Issue before investigation once the target is known.
